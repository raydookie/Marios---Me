- form_for :dump, :url=>{:controller=>"leaderboard", :action=>"import_csv"}, :html => { :multipart => true } do |f|
  %table
    %tr
      %td Select CSV File
      %td= f.file_field :file
    %tr
      %td= submit_tag 'Submit'
